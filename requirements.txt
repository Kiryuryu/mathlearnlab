# MathLearnLab — Production runtime dependencies
# Used by server/*.py at runtime. Notebook/dev-only deps live in requirements-dev.txt.

# ── Web Framework ──
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic-settings>=2.2,<3
python-multipart>=0.0.9,<1

# ── Auth ──
python-jose[cryptography]>=3.3,<4

# ── AI Client (DeepSeek via OpenAI-compatible API) ──
openai>=1.0,<2

# ── Database ──
pymysql>=1.1,<2
