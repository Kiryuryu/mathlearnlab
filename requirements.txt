# MathLearnLab — Full dependencies

# ── Web Framework ──
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic-settings>=2.2,<3
python-multipart>=0.0.9,<1
aiofiles>=24.1,<25

# ── Auth ──
python-jose[cryptography]>=3.3,<4

# ── AI Client (DeepSeek via OpenAI-compatible API) ──
openai>=1.0,<2

# ── Image processing ──
Pillow>=10.0,<11

# ── Core numerical & symbolic (notebooks) ──
numpy>=1.26,<2
scipy>=1.13,<2
sympy>=1.14,<2

# ── Visualization (notebooks) ──
matplotlib>=3.8,<4
plotly>=5.18,<6
seaborn>=0.13,<1

# ── Interactive widgets (notebooks) ──
ipywidgets>=8.1,<9
ipympl>=0.9,<1

# ── Jupyter ecosystem ──
jupyterlab>=4.0,<5
notebook>=7.0,<8
nbformat>=5.9,<6

# ── Animation support ──
ipython>=8.20,<9

# ── Database ──
pymysql>=1.1,<2
